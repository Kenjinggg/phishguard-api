# Required: API key for authenticating extension requests
PHISHGUARD_API_KEY=your-secret-key-here

# Optional: Flask debug mode (default: false)
FLASK_DEBUG=false

# Optional: Flask host (default: 0.0.0.0)
FLASK_HOST=0.0.0.0

# Optional: Flask port (default: 5000)
FLASK_PORT=5000
